# Excel嵌入图片功能使用说明

## 功能概述

MatrixFlow现在支持在Excel模板表格中**直接插入图片文件**，而不仅仅是填写图片路径。这使得模板管理更加直观和便捷。

## 支持的列

以下列支持嵌入图片：
- **底图**
- **星盘图片**

## 使用方法

### 方式一：填写图片路径（原有方式）

在单元格中直接输入图片的绝对路径：

```
C:\Users\xxx\Pictures\background.jpg
/Users/mac/Desktop/images/chart.png
```

### 方式二：插入图片文件（新功能）✨

1. 在Excel中打开模板表格
2. 选中需要插入图片的单元格（底图或星盘图片列）
3. 点击Excel菜单：**插入 → 图片 → 此设备**
4. 选择图片文件并插入
5. 调整图片大小和位置，确保图片左上角在目标单元格内

**注意事项：**
- 图片必须插入到对应的单元格内（图片左上角位置决定归属）
- 支持的图片格式：PNG, JPG, JPEG, WEBP, BMP
- 单元格可以留空，系统会自动检测并提取嵌入的图片

## 工作原理

1. **优先级**：如果单元格既有文字路径又有嵌入图片，优先使用文字路径
2. **自动提取**：当单元格为空时，系统自动检测并提取嵌入的图片
3. **临时存储**：提取的图片会保存到系统临时目录：`/tmp/matrixflow-excel-images/`
4. **自动清理**：系统会在适当时候清理临时文件

## 示例

### Excel表格示例

| 模版名 | 脚本 | 底图 | 星盘图片 | BGM素材 | 发布文案 | 发布日期 | 当天发布时间 | 作品名字 |
|--------|------|------|----------|---------|----------|----------|--------------|----------|
| Stella纯文案 | 脚本内容... | /path/to/bg.jpg | /path/to/chart.png | /path/to/bgm.mp3 | 文案内容 | 2026-12-31 | 14:30 | 作品1 |
| luna纯文案 | 脚本内容... | 🖼️ *[嵌入图片]* | 🖼️ *[嵌入图片]* | /path/to/bgm.mp3 | 文案内容 | 2027-01-15 | 16:00 | 作品2 |

### 在Excel中插入图片

```
第2行：使用路径方式
第3行：使用嵌入图片方式
  - 在"底图"列单元格上插入图片
  - 在"星盘图片"列单元格上插入图片
```

## 测试功能

### 1. 创建测试Excel文件

```bash
node create-test-excel.js
```

这会创建一个名为 `test-embedded-images.xlsx` 的测试文件，其中包含：
- 第2行：使用路径方式的数据
- 第3行：使用嵌入图片的数据

### 2. 测试图片提取

```bash
node test-embedded-image.js test-embedded-images.xlsx
```

这会显示详细的图片提取过程和结果。

### 3. 在应用中测试

1. 启动MatrixFlow应用
2. 进入"自动化发布"页面
3. 导入测试Excel文件
4. 查看预览，确认图片已正确提取

## 技术细节

### 实现位置

- **主要代码**：`electron/services/AutomationWorkbookService.ts`
- **提取函数**：`extractEmbeddedImage()`
- **使用的库**：ExcelJS

### 图片提取逻辑

```typescript
// 1. 检查单元格文本路径
let imagePath = cellText(get('星盘图片'));

// 2. 如果没有文本路径，尝试提取嵌入图片
if (!imagePath) {
  const embeddedImage = extractEmbeddedImage(
    worksheet,
    rowNumber,
    columnNumber,
    workbook
  );
  if (embeddedImage) {
    imagePath = embeddedImage;
  }
}
```

### 支持的图片格式

根据 `JianyingTemplateService.ts` 的定义：
```typescript
const IMAGE_EXTENSIONS = new Set([
  '.jpg',
  '.jpeg',
  '.png',
  '.webp',
  '.bmp'
]);
```

## 常见问题

### Q1: 图片没有被识别？

**检查项：**
- 图片左上角是否在目标单元格内
- 图片格式是否支持（PNG/JPG/JPEG/WEBP/BMP）
- 单元格是否为空（有文字路径会优先使用路径）

### Q2: 可以同时使用路径和嵌入图片吗？

**答：** 可以，但系统会优先使用文字路径。建议二选一使用：
- 需要灵活更换图片 → 使用路径
- 需要随Excel一起分发 → 使用嵌入图片

### Q3: 嵌入图片会增加Excel文件大小吗？

**答：** 会的。嵌入的图片会保存在Excel文件内，文件大小会相应增加。如果有大量高分辨率图片，建议使用路径方式。

### Q4: 临时文件什么时候清理？

**答：** 提取的图片保存在系统临时目录，通常会在：
- 系统重启时自动清理
- 磁盘清理时清理
- 应用关闭后可能被操作系统清理

## 优势对比

| 特性 | 路径方式 | 嵌入图片方式 |
|------|----------|--------------|
| Excel文件大小 | ✅ 小 | ❌ 大 |
| 便携性 | ❌ 需要同时传输图片文件 | ✅ 一个文件包含所有内容 |
| 更新图片 | ✅ 直接替换文件即可 | ❌ 需要重新插入 |
| 直观性 | ❌ 只能看到路径 | ✅ 可以直接在Excel中预览 |
| 跨平台 | ❌ Windows/Mac路径不同 | ✅ 嵌入图片无路径问题 |

## 最佳实践

1. **小规模测试**：使用嵌入图片方式，方便分享和演示
2. **大规模生产**：使用路径方式，便于批量管理和更新
3. **混合使用**：底图用路径（固定资源），星盘图片用嵌入（变化内容）
4. **团队协作**：使用嵌入图片，避免路径同步问题

## 更新日志

### v0.3.2 (2026-08-07)
- ✨ 新增：支持在Excel中直接插入图片文件
- ✨ 新增：自动提取嵌入图片到临时目录
- ✨ 新增：底图和星盘图片列支持嵌入图片
- 🔧 优化：优先使用文字路径，向后兼容

## 技术支持

如有问题，请：
1. 查看日志：应用菜单 → 开发者工具 → Console
2. 运行测试脚本：`node test-embedded-image.js <excel文件路径>`
3. 提交Issue：https://github.com/your-repo/issues

---

**提示**：此功能已完全测试并可用于生产环境。如遇到问题，请参考测试脚本进行排查。
